server {
    listen 80;
    server_name https://wealthwise-front.onrender.com;

    root /usr/share/nginx/html/browser;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /index.html;

    location ~ /\. {
        deny all;
    }
}
